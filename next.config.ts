import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // Turbopack disabled — removes the floating "N" indicator in dev mode
  // experimental: { turbo: {} }  ← intentionally omitted
}

export default nextConfig
